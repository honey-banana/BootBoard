<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.projectWG.board.mapper.BoardMapper"> <!-- BoardMapper interface 위치 -->
<!--    <select id="selectByNo" parameterType="int" resultType="com.projectWG.board.vo.Board">-->
<!--        SELECT-->
<!--            *-->
<!--        FROM BOARD-->
<!--        WHERE board_no = #{boardNo}-->
<!--    </select>-->
    <select id="selectAll" resultType="com.projectWG.board.vo.Board">
        SELECT
            *
        FROM BOARD
        ORDER BY board_no DESC
    </select>
</mapper>